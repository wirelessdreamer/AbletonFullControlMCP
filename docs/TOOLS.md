# AbletonMCP tools reference

Auto-generated by `python -m ableton_mcp.scripts.generate_tools_doc`.
 Do not hand-edit — your changes will be overwritten.

**244 tools** across **31 categories**.

## Table of contents

- [Transport (live_*)](#transport-live_) — 28 tools
- [Tracks (track_*)](#tracks-track_) — 22 tools
- [Clips (clip_*)](#clips-clip_) — 31 tools
- [Scenes (scene_*)](#scenes-scene_) — 11 tools
- [Cue points (cue_point_*)](#cue-points-cue_point_) — 6 tools
- [View (view_*)](#view-view_) — 5 tools
- [Arrangement (arrangement_*)](#arrangement-arrangement_) — 5 tools
- [Routing (routing_*)](#routing-routing_) — 5 tools
- [Devices (device_*)](#devices-device_) — 10 tools
- [MIDI mapping & files (midi_*)](#midi-mapping-files-midi_) — 7 tools
- [Audio analysis (audio_*)](#audio-analysis-audio_) — 2 tools
- [Listeners (listen_*)](#listeners-listen_) — 9 tools
- [High-level operations (op_*)](#high-level-operations-op_) — 10 tools
- [Browser (browser_*)](#browser-browser_) — 6 tools
- [Render (render_*)](#render-render_) — 2 tools
- [Sound modeling (sound_*)](#sound-modeling-sound_) — 14 tools
- [Knowledge / RAG (ableton_*)](#knowledge-rag-ableton_) — 2 tools
- [Suno (suno_*)](#suno-suno_) — 2 tools
- [Bench (bench_*)](#bench-bench_) — 4 tools
- [Bounce (bounce_*)](#bounce-bounce_) — 10 tools
- [Gen (gen_*)](#gen-gen_) — 2 tools
- [Inventory (inventory_*)](#inventory-inventory_) — 6 tools
- [Mix (mix_*)](#mix-mix_) — 9 tools
- [Pco (pco_*)](#pco-pco_) — 3 tools
- [Preset (preset_*)](#preset-preset_) — 6 tools
- [Project (project_*)](#project-project_) — 1 tools
- [Sections (sections_*)](#sections-sections_) — 2 tools
- [Shape (shape_*)](#shape-shape_) — 4 tools
- [Song (song_*)](#song-song_) — 7 tools
- [Stems (stems_*)](#stems-stems_) — 2 tools
- [Structure (structure_*)](#structure-structure_) — 11 tools

## Transport (live_*)

### `live_capture_midi`

Capture the MIDI buffer into a clip on every armed MIDI track.

**Parameters**

_No parameters._


### `live_continue`

Resume playback from where it stopped.

**Parameters**

_No parameters._


### `live_get_quantization_legend`

Return the integer→label mapping for clip trigger / MIDI recording quantization.

**Parameters**

_No parameters._


### `live_get_state`

Comprehensive transport snapshot.

**Parameters**

_No parameters._


### `live_jump_by`

Nudge arrangement playback by a relative beat offset.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `beats` | `number` | yes |  |


### `live_jump_to_beat`

Jump arrangement playback to absolute time in beats.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `beats` | `number` | yes |  |


### `live_ping`

Verify AbletonOSC is reachable. Call this first if anything else fails.

**Parameters**

_No parameters._


### `live_play`

Start playback from current position.

**Parameters**

_No parameters._


### `live_redo`

Redo the last undone edit.

**Parameters**

_No parameters._


### `live_session_record`

Toggle session record (the big record button).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `on` | `boolean` | yes |  |


### `live_set_arrangement_overdub`

Toggle arrangement overdub.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `on` | `boolean` | yes |  |


### `live_set_back_to_arranger`

Toggle the 'Back to Arrangement' button.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `on` | `boolean` | yes |  |


### `live_set_clip_trigger_quantization`

Set the global clip trigger quantization (Live enum int 0..13; 0=None, 4=1/1, etc.).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `value` | `integer` | yes |  |


### `live_set_groove_amount`

Set the global groove amount (0.0..1.0).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `amount` | `number` | yes |  |


### `live_set_loop`

Configure the arrangement loop.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `enabled` | `boolean` | yes |  |
| `start_beats` | `number | null` | no | None |
| `length_beats` | `number | null` | no | None |


### `live_set_metronome`

Enable or disable the metronome.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `on` | `boolean` | yes |  |


### `live_set_midi_recording_quantization`

Set the global MIDI recording quantization (same enum as trigger quantization).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `value` | `integer` | yes |  |


### `live_set_nudge`

Toggle the nudge_up or nudge_down button (used to manually push tempo).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `direction` | `string` | yes |  |
| `on` | `boolean` | yes |  |


### `live_set_punch`

Toggle punch-in and/or punch-out.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `in_` | `boolean | null` | no | None |
| `out` | `boolean | null` | no | None |


### `live_set_record_mode`

Set the global record mode (Live's internal int — see Live API).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `mode` | `integer` | yes |  |


### `live_set_tempo`

Set the global tempo in BPM.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `bpm` | `number` | yes |  |


### `live_set_time_signature`

Set time signature (e.g. 7,8 for 7/8).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `numerator` | `integer` | yes |  |
| `denominator` | `integer` | yes |  |


### `live_show_message`

Display a status message in Ableton's status bar.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `message` | `string` | yes |  |


### `live_stop`

Stop transport.

**Parameters**

_No parameters._


### `live_stop_all_clips`

Stop every playing clip.

**Parameters**

_No parameters._


### `live_tap_tempo`

Tap the tempo (one tap; call repeatedly to actually change tempo).

**Parameters**

_No parameters._


### `live_trigger_session_record`

Trigger session recording on all armed tracks.

**Parameters**

_No parameters._


### `live_undo`

Undo the last edit.

**Parameters**

_No parameters._



## Tracks (track_*)

### `track_create_audio`

Create an audio track.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `at_index` | `integer` | no | -1 |
| `name` | `string | null` | no | None |


### `track_create_midi`

Create a MIDI track. at_index=-1 appends at the end.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `at_index` | `integer` | no | -1 |
| `name` | `string | null` | no | None |


### `track_create_return`

Create a return track.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `name` | `string | null` | no | None |


### `track_delete`

Delete a regular track by index.

        ``dry_run=True`` returns a structured summary of what would be
        deleted (track name, total track count before/after) WITHOUT
        actually deleting. Use for LLM-mediated confirmation workflows
        before destructive operations.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `dry_run` | `boolean` | no | False |


### `track_delete_return`

Delete a return track by its return-track index.

        ``dry_run=True`` returns what would be deleted without acting.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `return_track_index` | `integer` | yes |  |
| `dry_run` | `boolean` | no | False |


### `track_duplicate`

Duplicate a track by index.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `track_get`

Detailed snapshot of a single track including mixer state, monitoring, fired/playing slots.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `track_get_meters`

Return current output meters (left, right, mono level) for a track. Sampled instantaneously.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `track_get_send`

Get the current value of a send.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `send_index` | `integer` | yes |  |


### `track_list`

Summarise every track: index, name, mute, solo, arm, grouped/foldable/visible flags, IO capability.

**Parameters**

_No parameters._


### `track_set_arm`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `armed` | `boolean` | yes |  |


### `track_set_color`

Set track color as a 24-bit RGB int (e.g. 0xFF8800).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `color` | `integer` | yes |  |


### `track_set_color_index`

Set track color from Live's palette (0..69).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `color_index` | `integer` | yes |  |


### `track_set_fold`

Fold/unfold a group track.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `folded` | `boolean` | yes |  |


### `track_set_monitoring`

Set monitoring state on an audio/midi track. 0=In, 1=Auto, 2=Off.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `state` | `integer` | yes |  |


### `track_set_mute`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `muted` | `boolean` | yes |  |


### `track_set_name`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `name` | `string` | yes |  |


### `track_set_pan`

Set track panning (-1.0 left .. 1.0 right).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `pan` | `number` | yes |  |


### `track_set_send`

Set the level of a send (0.0..1.0) on a track.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `send_index` | `integer` | yes |  |
| `value` | `number` | yes |  |


### `track_set_solo`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `solo` | `boolean` | yes |  |


### `track_set_volume`

Set track volume (0.0..1.0; ~0.85 ≈ 0 dB).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `volume` | `number` | yes |  |


### `track_stop_all_clips`

Stop all clips on a track.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |



## Clips (clip_*)

### `clip_add_notes`

Add MIDI notes to a clip.

        Each note: {pitch:int, start:float (beats), duration:float (beats),
        velocity:int 1..127, mute:bool (optional)}.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `notes` | `array<object>` | yes |  |


### `clip_create_midi`

Create an empty MIDI clip in a Session slot.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `length_beats` | `number` | no | 4.0 |


### `clip_delete`

Delete a clip from a Session slot.

        ``dry_run=True`` returns whether the slot currently has a clip
        and what its name is, WITHOUT deleting. Useful for asking the
        user "are you sure?" before a destructive call.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `dry_run` | `boolean` | no | False |


### `clip_duplicate_loop`

Duplicate the clip's loop region to extend its content.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |


### `clip_fire`

Trigger a clip.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |


### `clip_get`

Detailed snapshot of a single clip (covers most LOM clip properties).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |


### `clip_get_notes`

Return MIDI notes in a clip. Optional filters constrain pitch and time ranges.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `start_pitch` | `integer | null` | no | None |
| `pitch_span` | `integer | null` | no | None |
| `start_time` | `number | null` | no | None |
| `time_span` | `number | null` | no | None |


### `clip_list`

List clips on a track (Session view): slot index, name, length, color.

        AbletonOSC's reply for ``/live/track/get/clips/*`` is shaped
        ``(track_id, val_slot_0, val_slot_1, ..., val_slot_N)`` — a flat
        tuple with one entry per session clip slot, ``None`` for empty
        slots (see ``track.py`` in AbletonOSC). Empty slots are filtered
        out of the result; index in the returned list is the slot index.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `clip_remove_notes`

Remove notes from a clip. Defaults to removing all notes when no range given.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `start_pitch` | `integer | null` | no | None |
| `pitch_span` | `integer | null` | no | None |
| `start_time` | `number | null` | no | None |
| `time_span` | `number | null` | no | None |


### `clip_set_color`

Set clip color as a 24-bit RGB int.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `color` | `integer` | yes |  |


### `clip_set_color_index`

Set clip color by Live palette index (0..69).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `color_index` | `integer` | yes |  |


### `clip_set_gain`

Set clip gain (audio clip; 0..1, 0.5 ≈ 0 dB).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `gain` | `number` | yes |  |


### `clip_set_launch_mode`

Set launch mode: 0=Trigger, 1=Gate, 2=Toggle, 3=Repeat.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `mode` | `integer` | yes |  |


### `clip_set_launch_quantization`

Set per-clip launch quantization (Live enum int; 0=Global).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `value` | `integer` | yes |  |


### `clip_set_legato`

Toggle legato launch behavior.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `legato` | `boolean` | yes |  |


### `clip_set_loop`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `loop_start` | `number` | yes |  |
| `loop_end` | `number` | yes |  |


### `clip_set_markers`

Set the clip's start/end markers in beats.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `start_marker` | `number` | yes |  |
| `end_marker` | `number` | yes |  |


### `clip_set_muted`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `muted` | `boolean` | yes |  |


### `clip_set_name`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `name` | `string` | yes |  |


### `clip_set_pitch`

Set clip transposition (audio clip): semitones (-48..48) and fine cents (-50..50).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `semitones` | `integer` | no | 0 |
| `cents` | `number` | no | 0.0 |


### `clip_set_position`

Set the clip's position (in beats from clip start).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `position` | `number` | yes |  |


### `clip_set_ram_mode`

Toggle RAM mode for an audio clip.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `ram` | `boolean` | yes |  |


### `clip_set_velocity_amount`

Set the clip's velocity amount (0..1).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `amount` | `number` | yes |  |


### `clip_set_warp`

Enable/disable warping on an audio clip.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `warping` | `boolean` | yes |  |


### `clip_set_warp_mode`

Set warp mode: 0=Beats, 1=Tones, 2=Texture, 3=Re-Pitch, 4=Complex, 5=Complex Pro.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `mode` | `integer` | yes |  |


### `clip_slot_duplicate_to`

Duplicate a clip from one slot to another (target track must be compatible).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `source_track` | `integer` | yes |  |
| `source_clip` | `integer` | yes |  |
| `target_track` | `integer` | yes |  |
| `target_clip` | `integer` | yes |  |


### `clip_slot_fire`

Fire a slot (will trigger its clip or stop column depending on configuration).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |


### `clip_slot_get_stop_button`

Check whether a slot displays a stop button.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |


### `clip_slot_has_clip`

Check whether a slot contains a clip.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |


### `clip_slot_set_stop_button`

Show / hide the stop button on a slot.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `on` | `boolean` | yes |  |


### `clip_stop`

Stop a clip.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |



## Scenes (scene_*)

### `scene_create`

Create a new scene. at_index=-1 appends.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `at_index` | `integer` | no | -1 |
| `name` | `string | null` | no | None |


### `scene_delete`

Delete a scene by index.

        ``dry_run=True`` returns the scene's name + total scene count
        before/after, WITHOUT deleting.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `scene_index` | `integer` | yes |  |
| `dry_run` | `boolean` | no | False |


### `scene_duplicate`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `scene_index` | `integer` | yes |  |


### `scene_fire`

Launch every clip on a scene.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `scene_index` | `integer` | yes |  |


### `scene_fire_as_selected`

Select a scene and launch it (advances the selection per Live's setting).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `scene_index` | `integer` | yes |  |


### `scene_fire_selected`

Launch the currently selected scene.

**Parameters**

_No parameters._


### `scene_list`

Return every scene with name, color, is_empty, is_triggered, optional tempo/signature overrides.

**Parameters**

_No parameters._


### `scene_set_color_index`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `scene_index` | `integer` | yes |  |
| `color_index` | `integer` | yes |  |


### `scene_set_name`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `scene_index` | `integer` | yes |  |
| `name` | `string` | yes |  |


### `scene_set_tempo`

Set scene tempo override.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `scene_index` | `integer` | yes |  |
| `bpm` | `number` | yes |  |
| `enabled` | `boolean` | no | True |


### `scene_set_time_signature`

Set scene time signature override.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `scene_index` | `integer` | yes |  |
| `numerator` | `integer` | yes |  |
| `denominator` | `integer` | yes |  |
| `enabled` | `boolean` | no | True |



## Cue points (cue_point_*)

### `cue_point_add_or_delete`

Toggle a cue point at the current playback position (Live's [Set] / [Delete] action).

**Parameters**

_No parameters._


### `cue_point_jump`

Jump arrangement playback to a numbered cue point.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `cue_point_index` | `integer` | yes |  |


### `cue_point_jump_next`

_(no description)_

**Parameters**

_No parameters._


### `cue_point_jump_prev`

_(no description)_

**Parameters**

_No parameters._


### `cue_point_set_name`

Rename a cue point by index.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `cue_point_index` | `integer` | yes |  |
| `name` | `string` | yes |  |


### `cue_points_list`

Return all cue points (locators) in the arrangement: name + time.

**Parameters**

_No parameters._



## View (view_*)

### `view_get_selection`

Return the currently selected scene, track, clip, and device.

**Parameters**

_No parameters._


### `view_select_clip`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `scene_index` | `integer` | yes |  |


### `view_select_device`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |


### `view_select_scene`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `scene_index` | `integer` | yes |  |


### `view_select_track`

_(no description)_

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |



## Arrangement (arrangement_*)

### `arrangement_clips_list`

List all clips placed on a track in the Arrangement view, with name, length, start_time.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `arrangement_find_sections`

Find regions where ``track_index`` is featured (e.g. "the solo").

        Layer 1.2 of the mix-aware shaping stack. The motivating use case
        is *"the lead doesn't cut through during the solo"* — to fix that,
        the system first needs to locate the solo. This tool answers the
        "where on the timeline" question.

        Three detection strategies:

        - ``method="clip_name"`` — match each clip's name against
          ``keywords`` (default: ``solo``, ``lead``, ``break``,
          ``bridge``, ``feature``, ``fill``, ``interlude``).
          Confidence 1.0 — user labeled it.
        - ``method="clip_overlap"`` — find clips on the focal track
          where at most ``max_overlapping_other_tracks`` other tracks
          are playing simultaneously (a drop-out solo arrangement).
          Confidence 0.5..0.9 based on how alone the focal is.
        - ``method="auto"`` (default) — try clip-name first; if it
          returns nothing, fall back to clip-overlap.
        - ``method="both"`` — run both, merge overlapping detections.

        Returns:
            ``{status, focal_track_index, method, sections: [...]}``.
            Each section has ``start_beats``, ``end_beats``,
            ``length_beats``, ``confidence`` in [0, 1], ``kind``,
            ``details`` (detector-specific).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `method` | `string` | no | 'auto' |
| `keywords` | `array<string> | null` | no | None |
| `min_length_beats` | `number` | no | 1.0 |
| `max_overlapping_other_tracks` | `integer` | no | 1 |


### `arrangement_insert_midi_clip`

Create an empty MIDI clip on the arrangement timeline.

        - ``track_index`` must point to a MIDI track. Audio tracks raise.
        - ``position_beats`` is the start position in beats (0-based).
          Negative values are rejected.
        - ``length_beats`` is the clip length in beats (default 4 = one bar in 4/4).

        Live's LOM rejects overlapping clips on the same track, so the
        target [position, position+length] range must not intersect any
        existing arrangement clip on that track. We surface Live's error
        as ``status="error"`` rather than raising.

        Returns the new clip's ``clip_index`` so the caller can follow
        up with ``clip_*`` setters (e.g. notes, color, name) via the
        existing arrangement-clip tool surface.

        Requires bridge version 1.4.0+; older bridges raise
        :class:`AbletonBridgeOutdated` with the install hint via the
        handshake added in PR #11.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `position_beats` | `number` | yes |  |
| `length_beats` | `number` | no | 4.0 |


### `arrangement_move_clip`

Move an arrangement clip to a new start position on the timeline.

        Live 11+ exposes ``Clip.move(beats_delta)`` as the only way to
        relocate a clip — we compute the delta from the current
        ``start_time`` and call it. The result includes the actual
        post-move position (Live may snap to grid quantization).

        Live's LOM rejects moves that would overlap an existing clip on
        the same track; we surface that as ``status="error"``.

        Requires bridge version 1.4.0+ (Live 11+ in turn, where
        ``Clip.move`` exists).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `new_position_beats` | `number` | yes |  |


### `arrangement_summary`

High-level snapshot of the arrangement: tempo, length, signature, num tracks/scenes.

**Parameters**

_No parameters._



## Routing (routing_*)

### `routing_available_inputs`

List input routing types and channels available for a track.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `routing_available_outputs`

List output routing types and channels available for a track.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `routing_get`

Get the current input/output routing for a track.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `routing_set_input`

Set a track's input routing by name (must match an available type / channel).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `type_name` | `string` | yes |  |
| `channel_name` | `string | null` | no | None |


### `routing_set_output`

Set a track's output routing by name (must match an available type / channel).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `type_name` | `string` | yes |  |
| `channel_name` | `string | null` | no | None |



## Devices (device_*)

### `device_get_parameter_string`

Get a parameter's display string (e.g. '440 Hz' instead of raw float).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |
| `parameter_index` | `integer` | yes |  |


### `device_get_parameters`

Return all parameters of a device with current value, min, max, quantized flag.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |


### `device_list`

List devices on a track: index, name, class_name, type.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `device_schema_for_track_device`

Fetch the device class_name from Live and return the matching schema (or a helpful 'no schema' message).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |


### `device_schema_get`

Return the full canonical schema for a device by its LOM ``class_name``.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `class_name` | `string` | yes |  |


### `device_schema_list`

List catalogued devices. ``device_type`` filters by 'instrument' / 'audio_effect' / 'midi_effect' / 'drum' / 'utility' / 'rack'.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `device_type` | `string | null` | no | None |


### `device_schema_recommended_sweep_params`

Just the params we recommend for sweeping on a given device.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `class_name` | `string` | yes |  |


### `device_schema_search`

Fuzzy search the catalogue by name / description / category / parameter name.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `query` | `string` | yes |  |
| `limit` | `integer` | no | 10 |


### `device_set_parameter`

Set a single device parameter by index.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |
| `parameter_index` | `integer` | yes |  |
| `value` | `number` | yes |  |


### `device_set_parameter_by_name`

Set a device parameter by case-insensitive name match.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |
| `parameter_name` | `string` | yes |  |
| `value` | `number` | yes |  |



## MIDI mapping & files (midi_*)

### `midi_file_export_from_clip`

Export the notes of a Session clip to a .mid file. Uses live tempo if bpm is None.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `output_path` | `string` | yes |  |
| `bpm` | `number | null` | no | None |


### `midi_file_humanize`

Apply small random timing & velocity perturbations to a .mid file.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `path` | `string` | yes |  |
| `output_path` | `string` | yes |  |
| `timing_jitter_beats` | `number` | no | 0.02 |
| `velocity_jitter` | `integer` | no | 8 |
| `seed` | `integer | null` | no | None |


### `midi_file_load_into_clip`

Load a .mid file (single instrument) into an existing or new Session clip.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `path` | `string` | yes |  |
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `instrument_index` | `integer` | no | 0 |
| `replace_existing` | `boolean` | no | True |


### `midi_file_quantize`

Quantize a .mid file to a beat grid; strength=1.0 is full snap.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `path` | `string` | yes |  |
| `output_path` | `string` | yes |  |
| `grid_beats` | `number` | no | 0.25 |
| `strength` | `number` | no | 1.0 |


### `midi_file_summary`

Inspect a .mid file: tempo, instruments, note count per instrument, total length.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `path` | `string` | yes |  |


### `midi_file_transpose`

Transpose every non-drum note in a .mid file by N semitones (clamped to 0..127).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `path` | `string` | yes |  |
| `output_path` | `string` | yes |  |
| `semitones` | `integer` | yes |  |


### `midi_map_cc`

Map a MIDI CC (1-based channel, 0..127 CC) to a device parameter.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |
| `parameter_index` | `integer` | yes |  |
| `midi_channel` | `integer` | yes |  |
| `cc_number` | `integer` | yes |  |



## Audio analysis (audio_*)

### `audio_analyze`

Extract a feature summary from an audio file: tempo, key estimate, MFCC mean, spectral stats.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `path` | `string` | yes |  |
| `sr` | `integer` | no | 22050 |


### `audio_compare`

Compare two audio files via cosine similarity of MFCC means (1.0 = identical timbre).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `path_a` | `string` | yes |  |
| `path_b` | `string` | yes |  |
| `sr` | `integer` | no | 22050 |



## Listeners (listen_*)

### `listen_clip_playing_position`

Subscribe to a clip's playing position (in beats).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `max_buffer` | `integer` | no | 256 |


### `listen_device_parameter`

Subscribe to a single device parameter's value.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |
| `parameter_index` | `integer` | yes |  |
| `max_buffer` | `integer` | no | 256 |


### `listen_list`

List active listener handles.

**Parameters**

_No parameters._


### `listen_poll`

Drain queued events for a subscription. Returns at most `max_events`.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `handle` | `string` | yes |  |
| `max_events` | `integer` | no | 64 |


### `listen_scene`

Subscribe to a scene-level property (e.g. is_triggered, name).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `scene_index` | `integer` | yes |  |
| `property` | `string` | yes |  |
| `max_buffer` | `integer` | no | 256 |


### `listen_song`

Subscribe to a song-level property (e.g. tempo, is_playing, beat). Returns a handle.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `property` | `string` | yes |  |
| `max_buffer` | `integer` | no | 256 |


### `listen_stop`

Unsubscribe and free the handle.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `handle` | `string` | yes |  |


### `listen_track`

Subscribe to a track-level property (e.g. mute, volume, panning, fired_slot_index).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `property` | `string` | yes |  |
| `max_buffer` | `integer` | no | 256 |


### `listen_view`

Subscribe to a view selection (selected_scene or selected_track).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `property` | `string` | yes |  |
| `max_buffer` | `integer` | no | 256 |



## High-level operations (op_*)

### `op_consolidate_clip`

Consolidate the loop region of a clip into a single new clip.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |


### `op_crop_clip`

Crop a clip to its current loop region.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |


### `op_flatten_track`

Flatten a frozen track (replace the source with rendered audio).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `op_freeze_track`

Freeze a track (commit its devices to rendered audio).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `op_group_tracks`

Group a contiguous range of tracks into a new group track.

        Live's `Song.group_tracks(start, end)` requires the indices to be
        contiguous. If `group_name` is provided we attempt to rename the new
        group track afterwards (best-effort).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_indices` | `array<integer>` | yes |  |
| `group_name` | `string | null` | no | None |


### `op_new_set`

Create a new empty Live Set.

        Live's Python LOM does NOT expose ``Application.create_new_set``
        or any equivalent. Creating a new set is a UI-only File menu
        operation on every Live version through 11.3. Live 12 hasn't
        added a LOM hook either.

        Workarounds:

        1. **Manual** — File → New Live Set (Ctrl/Cmd-N).
        2. **AppleScript / pywinauto** — drive the menu from outside Live.
           Brittle, breaks across UI updates, and Live doesn't expose a
           stable handle. Not recommended.
        3. **Set template** — save a baseline empty .als and use Live's
           "Default Set" preference so new sets start clean. One-time setup,
           then File → New Live Set behaves like our `new_set` would.

        Closest LOM-supported alternative for "give me a fresh canvas":
        ``op_save_set`` (save current), then manually New Live Set, then
        come back to MCP control. Or just delete every track in the
        current set via ``track_delete`` in a loop — same effective state.

**Parameters**

_No parameters._


### `op_reverse_clip`

Reverse a clip.

        - **MIDI clips**: fully supported. Reads every note, inverts each
          note's start time (``new_start = clip_length - old_start -
          duration``), writes the result back. Velocity, duration, pitch,
          and mute flags are preserved.
        - **Audio clips**: Live 11's Python LOM does not expose
          ``Clip.reverse()`` — it's a UI-only command. Returns
          ``supported=false`` with a workaround in the ``workaround``
          field (right-click + Reverse in Live, or render an offline-
          reversed wav and re-import).

        The reply includes a ``kind`` field (``"midi"`` or ``"audio"``)
        so callers can route based on clip type.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |


### `op_save_set`

Save the current Live Set (Cmd-S).

        `path` is accepted for forward compatibility but Live's API does NOT
        support Save-As without the UI dialog, so it's currently ignored with
        a `save_as_supported: False` flag in the response.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `path` | `string | null` | no | None |


### `op_slice_clip_to_midi`

Slice an audio clip into a Drum Rack with one pad per slice.

        Live 11/12's Python LOM does NOT expose this — ``Slice to New MIDI
        Track`` is a UI command that internally creates a new MIDI track,
        a Drum Rack with one Simpler per slice point, and a MIDI clip
        triggering each pad. There's no LOM hook.

        Workarounds:

        1. **Manual** — right-click the audio clip in Live and pick "Slice
           to New MIDI Track". Use the slicing preset of your choice in
           the dialog (1/4 / 1/8 / 1/16 / transients / beats / regions).
        2. **Programmatic (more work)** — run onset detection externally
           (e.g. ``librosa.onset.onset_detect`` for transient slicing),
           create a new MIDI track via ``track_create_midi``, build a
           Drum Rack with N Simpler pads each pointing at a slice region
           of the source wav (via ``browser_load_device`` +
           ``browser_load_sample``), then write a MIDI clip with one
           note per pad triggered at the slice point. Heavy but possible
           today with existing MCP tools.
        3. **Max for Live** — devices like Ableton's own Slice or
           third-party slicers can do this without leaving Live.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `slicing_preset` | `string` | no | '16th' |


### `op_ungroup_track`

Ungroup a group track.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `group_track_index` | `integer` | yes |  |



## Browser (browser_*)

### `browser_get_tree`

Walk Live's browser. `depth` controls how many child levels are returned.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `depth` | `integer` | no | 2 |


### `browser_list_at_path`

List immediate children at a slash-delimited browser path.

        Examples: `"instruments"`, `"instruments/Operator"`, `"samples/Drums"`.
        Top-level categories: instruments, audio_effects, midi_effects, drums,
        sounds, samples, plugins, user_library, current_project, packs.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `path` | `string` | yes |  |


### `browser_load_device`

Load a device (instrument or effect) onto a track.

        `uri` may be either a slash-delimited browser path (preferred — what
        `browser_search` returns as `path`) or a Live browser URI.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `uri` | `string` | yes |  |
| `track_index` | `integer` | yes |  |


### `browser_load_drum_kit`

Load a drum rack onto a track. Accepts a path or URI as `browser_load_device`.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `uri` | `string` | yes |  |
| `track_index` | `integer` | yes |  |


### `browser_load_sample`

Load a sample onto an audio track. Optionally focus a specific clip slot first.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `uri` | `string` | yes |  |
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer | null` | no | None |


### `browser_search`

Substring-search Live's browser (case-insensitive). `category` narrows the scope.

        Returns up to 200 loadable items as `{name, path, is_loadable, uri}`.
        Use the returned `path` with `browser_load_device` / `browser_load_sample`.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `query` | `string` | yes |  |
| `category` | `string | null` | no | None |



## Render (render_*)

### `render_clip`

[Phase 2] Bounce a single Session clip to wav.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `clip_index` | `integer` | yes |  |
| `output_path` | `string` | yes |  |


### `render_master`

[Phase 2] Bounce the master bus to a wav file.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `output_path` | `string` | yes |  |
| `length_beats` | `number | null` | no | None |



## Sound modeling (sound_*)

### `sound_apply_descriptor`

Apply a single sound descriptor to a Live device using curated rules.

        Examples: ``descriptor="brighter"``, ``descriptor="aggressive"``,
        ``descriptor="warm"``. The rule set is keyed by the device's LOM
        class_name; if the device isn't covered (e.g. a third-party VST),
        returns ``status: "unsupported_device"``.

        ``intensity`` (0..1) scales how far each rule pushes the param.
        ``dry_run=True`` reports what would change without pushing OSC.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |
| `descriptor` | `string` | yes |  |
| `intensity` | `number` | no | 0.5 |
| `dry_run` | `boolean` | no | False |


### `sound_apply_descriptors`

Apply multiple descriptors across a track's devices.

        Pass ``device_index`` to limit to a single device, otherwise the
        rules walk the entire chain — devices without a rule set (VSTs,
        unsupported stocks) are noted in ``per_device[].status`` but don't
        fail the call.

        Opposing descriptors cancel by design ("warmer and brighter" with
        equal intensity yields a near-zero filter move).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |
| `descriptors` | `array<string>` | yes |  |
| `intensity` | `number` | no | 0.5 |
| `dry_run` | `boolean` | no | False |
| `device_index` | `integer | null` | no | None |


### `sound_describe`

Describe an audio file with ranked natural-language descriptors.

        Loads the wav with librosa, extracts the 32-dim feature vector via
        :func:`ableton_mcp.sound.features.extract_features`, then ranks every
        descriptor in the vocabulary by how well its anchors fit. Returns the
        top ``top_k`` matches + the raw features (for debugging).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `audio_path` | `string` | yes |  |
| `top_k` | `integer` | no | 8 |
| `sr` | `integer` | no | 22050 |


### `sound_describe_all_tracks`

Scan every audible track and summarise its sound.

**Parameters**

_No parameters._


### `sound_describe_track`

Musician-readable summary of one track's instrument + effect chain.

        Reads the device chain via OSC, matches each device's class_name
        against the schema catalogue and curated rule set, and returns
        a one-paragraph summary plus per-device records (display name,
        synthesis-architecture blurb, descriptors that map to its knobs).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer` | yes |  |


### `sound_descriptor_explain`

Return the full schema of one descriptor (anchors, aliases, opposite).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `label` | `string` | yes |  |


### `sound_descriptors_list`

List every known descriptor, optionally filtered by category.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `category` | `string | null` | no | None |


### `sound_design_coverage`

Report which devices and descriptors the curated rule catalogue covers.

        Useful for diagnostics — surfaces gaps so the LLM knows when to
        fall back to the heavier ``shape_apply`` (probe-dataset) workflow.

**Parameters**

_No parameters._


### `sound_explain_descriptor`

Return the curated rule body for a (device class, descriptor) pair.

        Use this to explain *why* a parameter was changed. For example,
        "brighter on Drift" returns the rule list that includes Filter
        Frequency (+1, 0.9) — so the LLM can say "I lifted the cutoff".

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `class_name` | `string` | yes |  |
| `descriptor` | `string` | yes |  |


### `sound_explain_parameter`

Sweep one parameter while holding the others at their midpoint.

        Reports per-dimension feature deltas across the sweep so callers can
        say "centroid +30%, flatness +20% — opens up the highs and adds noise".

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `parameter_name` | `string` | yes |  |
| `device_id` | `string` | no | 'synth_stub' |
| `track_index` | `integer | null` | no | None |
| `device_index` | `integer | null` | no | None |
| `param_ranges` | `object | null` | no | None |
| `steps` | `integer` | no | 11 |
| `sample_rate` | `integer` | no | 22050 |
| `duration_sec` | `number` | no | 2.0 |
| `top_dimensions` | `integer` | no | 6 |


### `sound_list_descriptors_for_device`

List the descriptors this device's rule set knows about.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `class_name` | `string` | yes |  |


### `sound_match`

Find probe cells whose features most closely match a target audio file.

        ``apply=True`` pushes the best params via OSC — only honoured when the
        dataset's device is a real Live device with ``track_index``/``device_index``
        provided. Set ``refine_top=True`` to run scipy.optimize starting from the
        kNN best for an extra polish (synth_stub only — real-device refinement
        needs LiveRenderer's capture pipeline, which is currently a stub).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target_audio_path` | `string` | yes |  |
| `dataset_path` | `string` | yes |  |
| `device_id` | `string | null` | no | None |
| `track_index` | `integer | null` | no | None |
| `device_index` | `integer | null` | no | None |
| `k` | `integer` | no | 5 |
| `refine_top` | `boolean` | no | False |
| `max_refine_iter` | `integer` | no | 20 |
| `sample_rate` | `integer` | no | 22050 |
| `apply` | `boolean` | no | False |


### `sound_probe_device`

Sweep a device's parameter space, extract features per cell, persist a probe dataset.

        Pass ``device_id="synth_stub"`` to run the in-process numpy synth (no Live
        required) — useful for testing the rest of the pipeline. For real Live
        devices, also pass ``track_index`` and ``device_index``; rendering will
        fail with NotImplementedError until a Resampling-based capture path is
        added to ``LiveRenderer``.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_index` | `integer | null` | no | None |
| `device_index` | `integer | null` | no | None |
| `device_id` | `string` | no | 'synth_stub' |
| `params` | `array<string> | null` | no | None |
| `param_ranges` | `object | null` | no | None |
| `steps_per_param` | `integer` | no | 5 |
| `strategy` | `string` | no | 'grid' |
| `sample_rate` | `integer` | no | 22050 |
| `duration_sec` | `number` | no | 2.0 |
| `midi_note` | `integer` | no | 60 |
| `output_path` | `string` | no | 'data/probes/probes.sqlite' |
| `seed` | `integer | null` | no | 0 |


### `sound_target_for_description`

Translate a free-text description into a target feature delta.

        ``description`` is a free-form phrase like "brighter, with more warmth".
        We parse out every known descriptor (with negation/intensifier cues),
        compute each one's delta against ``current_audio_path`` (if supplied),
        and combine. Returns the per-feature delta dict plus the parsed
        descriptors and, when audio was supplied, a preview of the target
        absolute feature values.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `description` | `string` | yes |  |
| `current_audio_path` | `string | null` | no | None |
| `intensity` | `number` | no | 1.0 |
| `sr` | `integer` | no | 22050 |



## Knowledge / RAG (ableton_*)

### `ableton_explain`

Retrieve grounded context for an Ableton how-to question.

        Returns the top `k` relevant snippets PLUS a `context` string that
        concatenates them with citations. The MCP client's LLM should use the
        `context` string as grounding when answering — this tool intentionally
        does not call an LLM itself.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `question` | `string` | yes |  |
| `k` | `integer` | no | 8 |


### `ableton_search_docs`

Search the local Ableton Live 11 manual + Cookbook index for snippets relevant to `query`.

        Returns the top `k` matching chunks with their score, chapter, and source URL.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `query` | `string` | yes |  |
| `k` | `integer` | no | 5 |



## Suno (suno_*)

### `suno_generate`

[Phase 6] Generate a song with Suno. Alias for gen_generate("suno", ...).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `prompt` | `string` | yes |  |
| `lyrics` | `string | null` | no | None |


### `suno_import_stems`

[Phase 6] Demucs-split an audio file and create one audio track per stem.

        Backwards-compat alias: combines stems_split + stems_import_to_live.
        Loading the wav into a clip still needs the Phase 2 browser bridge.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `audio_path` | `string` | yes |  |
| `target_track_start` | `integer` | no | -1 |
| `demucs_model` | `string` | no | 'htdemucs' |



## Bench (bench_*)

### `bench_describe`

Describe one bench synth: param ranges, defaults, recommended sweep set.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `synth_name` | `string` | yes |  |


### `bench_list_synths`

List every in-process bench synth with its full parameter schema.

**Parameters**

_No parameters._


### `bench_probe`

Run a full probe sweep across a bench synth and persist features.

        Uses the existing ``SweepPlanner`` + ``ProbeDataset`` so the resulting
        dataset is interchangeable with ``sound_match`` / ``sound_explain_parameter``
        queries — same on-disk schema as the real ``sound_probe_device``.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `synth_name` | `string` | yes |  |
| `params` | `array<string> | null` | no | None |
| `steps_per_param` | `integer` | no | 5 |
| `strategy` | `string` | no | 'grid' |
| `output_path` | `string` | no | 'data/bench/probes.sqlite' |
| `sample_rate` | `integer` | no | 22050 |
| `duration_sec` | `number` | no | 2.0 |
| `midi_note` | `integer` | no | 60 |
| `seed` | `integer | null` | no | 0 |


### `bench_render`

Render a bench synth to a wav file. Defaults fill any unspecified param.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `synth_name` | `string` | yes |  |
| `params` | `object | null` | no | None |
| `output_path` | `string` | no | 'data/bench/render.wav' |
| `sample_rate` | `integer` | no | 22050 |
| `duration_sec` | `number` | no | 2.0 |
| `midi_note` | `integer` | no | 60 |



## Bounce (bounce_*)

### `bounce_enabled`

Bounce every un-muted track (+ optionally master) as separate stems.

        Two pipelines, picked via ``mode``:

        - ``mode="resampling"`` (default) — realtime, one playback pass.
          ``warmup_sec`` defaults to 0.5 s here to prime samplers on
          fresh sessions. ``include_master=True`` adds a Resampling
          track for the full mix.
        - ``mode="freeze"`` — offline via ``Track.freeze()``, faster than
          realtime. ``duration_sec`` is ignored. ``include_master`` is
          not supported (freeze has no master equivalent). Requires the
          project to be saved. ``keep_frozen=True`` leaves tracks frozen
          after the bounce.

        Queries every track's mute state, skips muted ones and tracks
        without audio output, then runs the chosen pipeline on the rest.

        ``background=True`` returns ``{status: "started", job_id}``
        immediately and renders in a server-side job (coarse progress
        only for this operation); poll ``bounce_job_status(job_id)``.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `output_dir` | `string` | yes |  |
| `duration_sec` | `number` | yes |  |
| `include_master` | `boolean` | no | True |
| `encode_mp3` | `boolean` | no | True |
| `bitrate_kbps` | `integer` | no | 192 |
| `warmup_sec` | `number` | no | 0.5 |
| `mode` | `string` | no | 'resampling' |
| `keep_frozen` | `boolean` | no | False |
| `background` | `boolean` | no | False |


### `bounce_encode_mp3`

Encode a wav (or any ffmpeg-readable file) to mp3 via libmp3lame.

        Common bitrates: 128 (small), 192 (default — transparent for most music),
        256, 320 (max). Set `quality` to use VBR (0=best, 9=worst); overrides
        bitrate. Requires ffmpeg on PATH (https://ffmpeg.org/).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `wav_path` | `string` | yes |  |
| `mp3_path` | `string` | yes |  |
| `bitrate_kbps` | `integer` | no | 192 |
| `quality` | `integer | null` | no | None |


### `bounce_job_cancel`

Cancel a running background bounce job and wait for cleanup.

        The engine's cancellation contract restores Live's state (stops
        transport, disables record mode, deletes the temp track) before
        the job is marked ``cancelled``. Cancelling a finished job is a
        no-op that returns its final state.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `job_id` | `string` | yes |  |


### `bounce_job_list`

List background bounce jobs, newest first.

        Includes the running job (at most one — Live has a single
        transport) and up to the last 20 finished ones. Use to recover a
        job_id after losing track of it mid-batch.

**Parameters**

_No parameters._


### `bounce_job_status`

Status of a background bounce job.

        Returns `{status: "ok", job: {job_id, operation, state, progress,
        message, elapsed_sec, result, error}}`. ``state`` is one of
        ``running`` / ``done`` / ``error`` / ``cancelled``; when ``done``,
        ``result`` holds exactly the dict the foreground tool would have
        returned (wav paths, mp3s, ...).

        Batch pattern (e.g. 100-song render): load song → start bounce
        with ``background=True`` → poll this every ~10 s → on ``done``,
        verify ``result`` and start the next song. The registry is
        process-local: it empties if the MCP server restarts.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `job_id` | `string` | yes |  |


### `bounce_mix_stems`

Sum N stem WAVs into a single mix WAV. Pure offline operation.

        All stems must share a sample rate. Normalisation peak-limits to
        -headroom_db dBFS. Use this when you have stems on disk (from any
        source — `bounce_tracks`, freeze export, manual export from Live)
        and you also want a single full-mix file.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `stem_paths` | `array<string>` | yes |  |
| `output_path` | `string` | yes |  |
| `normalize` | `boolean` | no | True |
| `headroom_db` | `number` | no | 0.1 |


### `bounce_region`

Bounce a specific beat range of the arrangement.

        Region-bounded sibling of ``bounce_song`` / ``bounce_tracks``. The
        building block for the mix-aware shaping stack (see
        ``docs/MIX_AWARE_SHAPING.md``) — section analysis, masking
        diagnosis, and A/B verification all bounce a region rather than
        the whole song.

        - ``track_indices=None`` (default) → bounce the master mix for
          the region to ``<output_dir>/master_<start>-<end>.wav``.
        - ``track_indices=[...]`` → bounce per-track stems for the
          region to ``<output_dir>/stem_<idx>_<name>.wav``, captured in
          one playback pass.

        Wall-clock cost is real-time: the region's duration in seconds
        (computed from beats + Live's current tempo). 32 bars at 120 BPM
        is ~64 s of wall clock. For ultra-fast iteration on a saved
        project use ``bounce_tracks(mode="freeze")`` for whole-track
        stems and slice them post-hoc.

        Args:
            output_dir: directory to write the captured wav(s).
            start_beats: region start in beats (>= 0).
            end_beats: region end in beats (> start_beats).
            track_indices: tracks to capture, or None for master.
            encode_mp3 / bitrate_kbps / warmup_sec — see ``bounce_song``.
            background: return ``{status: "started", job_id}`` immediately
                and render in a server-side job with per-second progress;
                poll ``bounce_job_status(job_id)``.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `output_dir` | `string` | yes |  |
| `start_beats` | `number` | yes |  |
| `end_beats` | `number` | yes |  |
| `track_indices` | `array<integer> | null` | no | None |
| `encode_mp3` | `boolean` | no | True |
| `bitrate_kbps` | `integer` | no | 192 |
| `warmup_sec` | `number` | no | 0.0 |
| `background` | `boolean` | no | False |


### `bounce_region_all_active`

Bounce every un-muted, audio-producing track for a beat region.

        Layer 1.3 of the mix-aware shaping stack (see
        ``docs/MIX_AWARE_SHAPING.md``). Combines the track-selection
        logic of ``bounce_enabled`` with the region-bounded primitive
        ``bounce_region``. Produces per-track stems for the requested
        region — the input format Layer 2's spectral / masking analysis
        needs.

        Skips muted tracks, temp tracks from previous crashed bounces,
        and tracks without audio output (group folders, MIDI without
        instrument).

        ``background=True`` returns ``{status: "started", job_id}``
        immediately and renders in a server-side job (coarse progress
        only for this operation); poll ``bounce_job_status(job_id)``.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `output_dir` | `string` | yes |  |
| `start_beats` | `number` | yes |  |
| `end_beats` | `number` | yes |  |
| `encode_mp3` | `boolean` | no | True |
| `bitrate_kbps` | `integer` | no | 192 |
| `warmup_sec` | `number` | no | 0.0 |
| `background` | `boolean` | no | False |


### `bounce_song`

Bounce the entire song (master mix) to a wav (and optionally mp3).

        Realtime — takes ``duration_sec`` of wall-clock + ~1s overhead. Creates
        a temp audio track with input = Resampling, arms it, records the
        arrangement for the requested duration, copies the resulting wav to
        ``output_path``, then deletes the temp track. The user's existing
        tracks are not modified.

        If ``encode_mp3`` is True and ffmpeg is on PATH, also writes an mp3
        next to the wav at ``bitrate_kbps``.

        ``warmup_sec`` runs a brief no-record playback before the real
        capture to prime samplers + the audio engine. Use 0.3-0.5 s if the
        first second of your bounce comes out silent on fresh sessions
        (Live's samplers lazy-load on first trigger). Default 0 (off).

        ``background=True`` returns ``{status: "started", job_id}``
        immediately and renders in a server-side job — use for pieces
        longer than ~2 minutes or unattended batch renders; poll
        ``bounce_job_status(job_id)`` for progress and the final result.
        Only one bounce job can run at a time (Live has one transport).

        **Progress + cancellation** (foreground path): when invoked
        through an MCP client that supports ``notifications/progress``,
        this tool emits per-second progress updates during the recording
        phase (e.g. "recording 12.0/240.0 s") plus phase-boundary
        notifications at setup/harvest/cleanup boundaries. Cancellation
        via ``notifications/cancelled`` (or ``bounce_job_cancel`` for
        jobs) is handled cleanly: transport is stopped, record mode
        disabled, the temp track deleted, and the partial bounce raised
        as a CancelledError without leaving the user's session in a
        half-armed state.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `output_path` | `string` | yes |  |
| `duration_sec` | `number` | yes |  |
| `encode_mp3` | `boolean` | no | True |
| `bitrate_kbps` | `integer` | no | 192 |
| `warmup_sec` | `number` | no | 0.0 |
| `background` | `boolean` | no | False |


### `bounce_tracks`

Bounce specific tracks to per-track wav stems.

        Two pipelines, picked via ``mode``:

        - ``mode="resampling"`` (default) — realtime, one playback pass.
          Live records all listed tracks in parallel during a single
          playback pass; wall-clock is ``duration_sec`` regardless of
          track count. Captures each track's post-master-bus signal via a
          Resampling track. ``duration_sec`` and ``warmup_sec`` apply.
        - ``mode="freeze"`` — offline via ``Track.freeze()``, faster than
          realtime (~2-4× on a modern machine). Per-track wavs come
          straight out of Live's ``Samples/Freezing/`` folder.
          ``duration_sec`` is ignored (Live writes whatever length the
          track's clips span). Master-bus FX are NOT in the output —
          freeze captures pre-master signal. Requires the project to be
          saved (Live needs ``<project>/Samples/Freezing/``).
          ``keep_frozen=True`` leaves tracks frozen after the bounce;
          default unfreezes them to restore session state.

        Set ``include_master=True`` to also capture the master mix.
        ``include_master`` is only honored when ``mode="resampling"`` —
        freeze has no master equivalent.

        ``background=True`` returns ``{status: "started", job_id}``
        immediately and renders in a server-side job (coarse progress
        only for this operation); poll ``bounce_job_status(job_id)``.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_indices` | `array<integer>` | yes |  |
| `output_dir` | `string` | yes |  |
| `duration_sec` | `number` | yes |  |
| `include_master` | `boolean` | no | False |
| `encode_mp3` | `boolean` | no | True |
| `bitrate_kbps` | `integer` | no | 192 |
| `warmup_sec` | `number` | no | 0.0 |
| `mode` | `string` | no | 'resampling' |
| `keep_frozen` | `boolean` | no | False |
| `background` | `boolean` | no | False |



## Gen (gen_*)

### `gen_generate`

[Phase 6] Generate audio using the named provider.

        `provider` is one of the names from gen_list_providers (suno, musicgen,
        stable_audio). The resulting wav/mp3 is saved under `data/generated/`
        unless `output_path` is given. Raises a structured error dict with
        `status: not_configured` if the provider's env var/dep is missing.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `provider` | `string` | yes |  |
| `prompt` | `string` | yes |  |
| `lyrics` | `string | null` | no | None |
| `duration` | `number | null` | no | None |
| `output_path` | `string | null` | no | None |


### `gen_list_providers`

[Phase 6] List registered AI music generators and whether each is configured.

        A provider is "ready" when its required env var or local dependency is
        present — calling gen_generate with a non-ready provider raises
        GeneratorNotConfigured with a clear hint about what to set.

**Parameters**

_No parameters._



## Inventory (inventory_*)

### `inventory_introspect`

Load one device on a temp track, dump its parameters, delete the track.

        ``uri`` may be either a slash-delimited path or a Live URI.
        ``category`` controls whether a MIDI or audio probe track is
        created (instruments / midi_effects / drums / plugins → MIDI;
        audio_effects → audio).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `uri` | `string` | yes |  |
| `category` | `string` | yes |  |


### `inventory_load_manifest`

Return the saved manifest as a dict (raw round-trip).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `path` | `string` | yes |  |


### `inventory_match_manifest`

Re-run the schema matcher against a saved manifest.

        Useful after adding new canonical schemas: load the existing
        manifest, run :func:`match_to_schemas`, and return the coverage
        report without re-touching Live.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `manifest_path` | `string` | yes |  |


### `inventory_scan_all`

Bulk-walk the browser, introspect each loadable item, write a manifest.

        Set ``dry_run=True`` to skip the load-and-introspect step entirely
        — the result then lists what WOULD be probed without touching
        Live. Set ``max_items`` to cap the work for staged runs.

        Probe-able categories are instruments, audio_effects, midi_effects,
        drums, plugins. Other categories (samples, sounds) won't load
        usefully on a probe track and will return a clear error if asked.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `category` | `string` | no | 'instruments' |
| `out_path` | `string` | no | 'data/inventory/manifest.json' |
| `dry_run` | `boolean` | no | False |
| `max_items` | `integer | null` | no | None |


### `inventory_scan_browser`

Walk Live's browser tree without loading anything.

        Returns every item the bridge can see — including folders so the
        client can render structure if it wants. ``category`` narrows the
        scope to one of: instruments, audio_effects, midi_effects, drums,
        sounds, samples, plugins, user_library, current_project, packs.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `category` | `string | null` | no | None |


### `inventory_summary`

High-level stats: counts by category, schema coverage %, top plugins.

        Returns:
            * ``totals`` per browser category
            * ``coverage_pct`` — share of items with a full canonical schema
            * ``top_param_rich`` — 10 most parameter-rich plugins
            * ``no_schema`` — plugins with no canonical schema (truncated)

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `manifest_path` | `string` | yes |  |



## Mix (mix_*)

### `mix_apply_proposal`

Apply a proposal from ``mix_propose_at_region`` to Live.

        Per action, locates (or inserts) an EQ Eight on the target
        track, picks a free band, and sets filter type / frequency /
        gain / Q / on. ``dry_run=True`` returns the full plan WITHOUT
        making OSC writes -- ideal for confirming the change with the
        user before committing.

        Action kinds without a v1 apply path (``de_ess``,
        ``compress_attack``) land in the ``skipped`` list with a reason
        rather than failing the whole apply.

        Layer 4.2 of the mix-aware shaping stack.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `proposal` | `object` | yes |  |
| `dry_run` | `boolean` | no | False |


### `mix_classify_track_by_name`

Best-effort instrument-family classification from a track name.

        Returns the matched ``InstrumentMoneyBands`` entry (kick, bass,
        snare, hihat, lead_vocal, lead_guitar, rhythm_guitar, piano) or
        ``{"matched": false}`` if no alias fires. Most-specific match
        wins ("Bass Drum" -> kick, "Lead Guitar" -> lead_guitar).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `track_name` | `string` | yes |  |


### `mix_describe_intent`

Look up one mix-intent descriptor by canonical name or alias.

        ``intent`` is case- and whitespace-tolerant: "Cut Through",
        "cut-through", "cuts_through" all resolve to the same descriptor.
        Returns ``{"status": "error", "error": ...}`` if the word isn't
        a recognised intent.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `intent` | `string` | yes |  |


### `mix_list_intents`

List every mix-intent descriptor the propose / verify layers
        understand (cuts_through, buried, muddy, harsh, airy, ...).

        Use this to discover what words the user can ask for. Each
        descriptor has its band range, sign, action class, and a short
        human-readable description for context.

**Parameters**

_No parameters._


### `mix_masking_at_region`

For a focal track + region, score how much each other track is masking it.

        Bounces every active track for the region, identifies the focal's
        top ``top_n_money_bands`` highest-energy bands, then for each
        competitor scores per-band overlap weighted by perceptual band
        importance (presence band 2-5 kHz weighted highest).

        Returns ``{focal_track, focal_money_bands, competing_tracks:
        [{track_index, name, masking_score, per_band: [...]}, ...]}``.
        Layer 2.2 of the mix-aware shaping stack.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `focal_track_index` | `integer` | yes |  |
| `start_beats` | `number` | yes |  |
| `end_beats` | `number` | yes |  |
| `output_dir` | `string | null` | no | None |
| `target_sr` | `integer` | no | 22050 |
| `top_n_money_bands` | `integer` | no | 5 |
| `warmup_sec` | `number` | no | 0.0 |


### `mix_propose_at_region`

Generate a structured EQ proposal for a mix intent over a region.

        ``intent`` is a word like ``"cuts_through"``, ``"buried"``,
        ``"muddy"``, ``"harsh"``, ``"airy"``, ... (see
        :func:`mix_list_intents`). Case + whitespace tolerant.

        Bounces, analyzes masking, then dispatches by the descriptor's
        ``action_class`` to produce 0-N concrete actions (eq_cut,
        eq_boost, high_pass, high_shelf, low_shelf, ...). Each action
        carries a rationale string referencing the actual masking
        numbers. **Does NOT apply** -- pass the result to
        ``mix_apply_proposal``.

        Layer 4.1 of the mix-aware shaping stack.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `focal_track_index` | `integer` | yes |  |
| `intent` | `string` | yes |  |
| `start_beats` | `number` | yes |  |
| `end_beats` | `number` | yes |  |
| `output_dir` | `string | null` | no | None |
| `target_sr` | `integer` | no | 22050 |
| `warmup_sec` | `number` | no | 0.0 |


### `mix_snapshot_for_verification`

Take a baseline masking snapshot to be diffed later by ``mix_verify_intent``.

        Call this BEFORE ``mix_apply_proposal`` so you have a reference
        to compare against. Returns the same shape as
        ``mix_masking_at_region``. Pass the result back as the
        ``baseline_snapshot`` argument to ``mix_verify_intent``.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `focal_track_index` | `integer` | yes |  |
| `start_beats` | `number` | yes |  |
| `end_beats` | `number` | yes |  |
| `output_dir` | `string | null` | no | None |
| `target_sr` | `integer` | no | 22050 |
| `warmup_sec` | `number` | no | 0.0 |


### `mix_spectrum_at_region`

Per-track third-octave spectrum for every active track over a beat region.

        Bounces every un-muted, audio-producing track for the region
        (via L1.3), runs third-octave spectral analysis on each stem,
        and returns ``{region, bands, tracks: [{track_index, name,
        energy_db_per_band, peak_db, rms_db, spectral_centroid_hz,
        top_bands}, ...]}``.

        Layer 2.1 of the mix-aware shaping stack. The downstream layers
        build on this output to compute masking and propose changes.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `start_beats` | `number` | yes |  |
| `end_beats` | `number` | yes |  |
| `output_dir` | `string | null` | no | None |
| `target_sr` | `integer` | no | 22050 |
| `warmup_sec` | `number` | no | 0.0 |


### `mix_verify_intent`

Verify whether a mix intent was achieved after ``mix_apply_proposal``.

        Re-bounces the region, computes the same masking analysis as
        before, and diffs against ``baseline_snapshot`` (the result of
        ``mix_snapshot_for_verification`` taken BEFORE the apply).
        Returns ``{intent_achieved, regressed, summary,
        per_competitor_diffs, ...}`` with a natural-language summary
        suitable for showing the user.

        If ``baseline_snapshot`` is omitted the function returns the
        after-snapshot alone with ``baseline_missing=True`` -- no diff
        is possible without a baseline.

        Layer 5 of the mix-aware shaping stack.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `focal_track_index` | `integer` | yes |  |
| `intent` | `string` | yes |  |
| `start_beats` | `number` | yes |  |
| `end_beats` | `number` | yes |  |
| `baseline_snapshot` | `object | null` | no | None |
| `output_dir` | `string | null` | no | None |
| `target_sr` | `integer` | no | 22050 |
| `warmup_sec` | `number` | no | 0.0 |



## Pco (pco_*)

### `pco_find_song`

Search Planning Center songs by title.

        Returns `{status, matches: [{song_id, title, author, ccli_number,
        arrangements: [{arrangement_id, name, bpm, sequence_len,
        has_chord_chart}]}]}` — pass a song_id (and optionally an
        arrangement_id) on to pco_get_chart / sections_detect.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `query` | `string` | yes |  |


### `pco_get_chart`

Fetch the normalized chart for a song's arrangement.

        Returns `{status, chart: {song_id, arrangement_id, title,
        ccli_number, key, bpm, meter, sequence: [labels...],
        sections: [{label, role, lyrics}], warnings}}`. Defaults to the
        first arrangement; a warning lists the alternatives when several
        exist. Falls back to parsing the arrangement's ChordPro chord_chart
        when the sections endpoint carries no lyrics.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `song_id` | `string` | yes |  |
| `arrangement_id` | `string | null` | no | None |


### `pco_library_probe`

Sample the Planning Center song library and report data coverage.

        Run this FIRST when setting up section detection: it reports what
        fraction of songs have arrangement sequences, per-section lyrics,
        BPM, and meter — plus `schema_notes` describing the actual sections
        endpoint shape if it differs from expectations.

        Args:
            limit: number of songs to sample (throttled ~4/sec for rate limits).
            query: optional title filter, e.g. "goodness".

        Returns `{status, sampled, coverage: {pct_with_sequence,
        pct_with_section_lyrics, pct_with_bpm, pct_with_meter,
        pct_with_chord_chart, errors}, songs: [...], schema_notes, warnings}`.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `limit` | `integer` | no | 25 |
| `query` | `string | null` | no | None |



## Preset (preset_*)

### `preset_apply_to_live`

Push the named preset's params to (track_index, device_index) via OSC.

        Synth_stub presets are skipped (in-process — nothing to apply).
        Unmatched param names are reported, not errored.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `name` | `string` | yes |  |
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |


### `preset_discover`

Cluster a probe dataset into ``k`` discovered presets and add them to the library.

        Each cluster contributes one preset:
          - device_class = the source device_id from the dataset row
          - params       = the centroid-closest cell's params
          - tags         = top descriptors from the centroid feature vector
          - source       = ``"discovered"``

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `dataset_path` | `string` | yes |  |
| `k` | `integer` | no | 8 |
| `device_id` | `string | null` | no | None |
| `name_prefix` | `string | null` | no | None |


### `preset_get`

Return the full preset definition by name (case-insensitive).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `name` | `string` | yes |  |


### `preset_list`

List presets, optionally filtered by device_class / tag / source.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `device_class` | `string | null` | no | None |
| `tag` | `string | null` | no | None |
| `source` | `string | null` | no | None |


### `preset_render_synth_bench`

Render a preview wav for a synth_stub / synth_bench preset.

        Falls back to ``synth_stub.synth_render`` for synth_stub presets
        (and any preset whose params look like the stub schema). Returns a
        ``skipped`` status for real-Live device classes — nothing to render
        offline.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `name` | `string` | yes |  |
| `output_path` | `string` | yes |  |


### `preset_search`

Fuzzy match ``query`` against preset names, tags, and descriptions.

        Tokens are split on whitespace; a preset's score is the count of
        matched tokens (substring, case-insensitive) across name + tags +
        description, with a name-match bonus.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `query` | `string` | yes |  |
| `limit` | `integer` | no | 20 |



## Project (project_*)

### `project_describe`

One-call snapshot of the active Live session for question-answering.

        ``detail`` controls how much is included:

        - ``"summary"`` — project metadata + track names/indices only.
          Cheapest; use when you only need an overview.
        - ``"tracks"`` (default) — adds per-track mixer state, I/O
          capability flags, and clip counts. Right for questions about
          mute/solo state, track types, or which tracks have content.
        - ``"full"`` — also includes devices on each track. Use when the
          question is about plugins/instruments. Cost scales with track
          and device count.

        Returns:
            ``{live_version, bridge, project, tracks, scenes}``. The
            ``bridge`` block carries the version handshake result so the
            caller knows whether bridge-backed fields (devices) are
            reliable. Per-track read failures are logged at debug and
            surfaced as ``null`` for the affected field — the rest of
            the row still populates.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `detail` | `string` | no | 'tracks' |



## Sections (sections_*)

### `sections_detect`

Detect song sections in a recording by correlating its PCO chart.

        Pipeline: transcribe the Demucs vocal stem (stable-ts, cached) →
        fuzzy-match each chart section's lyrics against the transcript in
        time order (discovers the PERFORMED order, including extra repeats
        the chart doesn't list) → refine starts via known-text forced
        alignment → infer instrumental sections from vocal gaps → snap all
        boundaries to beat_this downbeats → write a sections.json sidecar.

        Prereqs: run stems_split on the audio first (the vocal stem is
        expected at data/stems/htdemucs_6s/<basename>/vocals.wav unless
        vocal_stem_path is given — this tool does NOT run Demucs itself),
        and set PCO_APP_ID/PCO_SECRET for chart access.

        Args:
            audio_path: the full-mix recording (beat grid + duration source).
            vocal_stem_path: override the conventional stem location.
            pco_song_id / pco_query: chart identity (id wins; query must
                match exactly one song or candidates are returned).
            arrangement_id: pick a specific arrangement (default: first).
            model_size: Whisper checkpoint (``small`` default).
            device: ``cuda``/``cpu``/None (auto).
            min_gap_bars: minimum instrumental gap worth marking, in bars.
            refine_with_alignment: disable to skip the forced-alignment
                refinement pass (faster, coarser starts).
            write_sidecar: write sections.json next to audio_path (or to
                output_path).
            output_path: explicit sidecar destination.

        Returns `{status, sections: [{label, display_name, role, kind,
        start_sec, end_sec, start_beats, confidence, status, ...}],
        performed_sequence, chart_sequence, tempo, warnings,
        sections_json_path}`.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `audio_path` | `string` | yes |  |
| `vocal_stem_path` | `string | null` | no | None |
| `pco_song_id` | `string | null` | no | None |
| `pco_query` | `string | null` | no | None |
| `arrangement_id` | `string | null` | no | None |
| `model_size` | `string` | no | 'small' |
| `device` | `string | null` | no | None |
| `min_gap_bars` | `number` | no | 2.0 |
| `refine_with_alignment` | `boolean` | no | True |
| `write_sidecar` | `boolean` | no | True |
| `output_path` | `string | null` | no | None |


### `sections_write_locators`

Write detected sections into Ableton as named arrangement locators.

        Loads a sections.json (explicit path, or next to audio_path) and
        creates one locator per section at its start_beats, idempotently:
        a same-named locator already within tolerance is skipped (re-runs
        are no-ops); a differently-named locator within 0.25 beats of a
        target is renamed in place. Live's cue API is a playhead toggle, so
        the transport is stopped and the playhead moves during the write.

        Contract: the practice WAV must start at `arrangement_start_beats`
        (default 0.0 = 1.1.1) and Live's tempo must match the detected BPM
        (the recording's musical beats must land on Live's grid). A tempo
        mismatch > 1 BPM refuses to write unless force=True.

        Args:
            sections_json_path: sidecar to load (wins over audio_path).
            audio_path: locate the sidecar next to this file instead.
            arrangement_start_beats: where WAV beat 0 sits in the arrangement.
            tolerance_beats: idempotency window for same-named locators.
            clear_existing: delete same-named locators at stale positions first.
            force: write even if the tempo guard fails.

        Returns `{status, added, renamed, skipped, deleted, failed,
        tempo_check, warnings}`.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `sections_json_path` | `string | null` | no | None |
| `audio_path` | `string | null` | no | None |
| `arrangement_start_beats` | `number` | no | 0.0 |
| `tolerance_beats` | `number` | no | 0.5 |
| `clear_existing` | `boolean` | no | False |
| `force` | `boolean` | no | False |



## Shape (shape_*)

### `shape_apply`

Predict + (optionally) apply the best-matching params to a Live device.

        ``dry_run=True`` (default) returns the prediction without touching
        Live. Set ``dry_run=False`` to push the top match's params via OSC.
        OSC failures are returned in the ``apply`` field — they don't raise.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `description` | `string` | yes |  |
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |
| `current_audio_path` | `string` | yes |  |
| `dataset_path` | `string` | yes |  |
| `k` | `integer` | no | 5 |
| `dry_run` | `boolean` | no | True |
| `sample_rate` | `integer` | no | 22050 |


### `shape_compare_apply`

"Make my device sound like *this* reference clip."

        Feature-extract the reference, kNN against the dataset, optionally
        push the top match. Mirrors ``shape_apply`` but driven by audio
        rather than text.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `reference_audio_path` | `string` | yes |  |
| `track_index` | `integer` | yes |  |
| `device_index` | `integer` | yes |  |
| `dataset_path` | `string` | yes |  |
| `k` | `integer` | no | 5 |
| `dry_run` | `boolean` | no | True |
| `sample_rate` | `integer` | no | 22050 |


### `shape_parse`

Debug helper — parse free-text into a structured ShapeRequest.

        Returns the canonical descriptors (label + intensity in {-2, -1, +1, +2}),
        any explicit feature targets ("centroid above 3000Hz"), an optional
        ``compare_to`` clause, and the device hint pulled from "on the lead" /
        "for the bass".

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `description` | `string` | yes |  |


### `shape_predict`

Predict the top-k param settings that match a free-text shaping request.

        Pipeline: parse the text → extract features from ``current_audio_path``
        → compute target features (semantics package if available, else
        fallback vocab) → kNN against the dataset.

        Does not touch Live. Use ``shape_apply`` to actually push the params.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `description` | `string` | yes |  |
| `current_audio_path` | `string` | yes |  |
| `dataset_path` | `string` | yes |  |
| `k` | `integer` | no | 5 |
| `sample_rate` | `integer` | no | 22050 |



## Song (song_*)

### `song_analyze`

Snapshot the active Live song: tempo, length, key estimate, scale hint.

        Bounces a 30-second slice (or shorter on a shorter song) and runs
        librosa chroma key estimation on it. Tempo + length come from Live
        directly via OSC. ``scale_hint`` reflects Live's UI Scale feature
        if the user has set it, otherwise None.

        Realtime cost: ~30 seconds for the slice bounce. Use this when you
        need to know what to transpose to/from, or to confirm tempo before
        a downstream operation.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `output_dir` | `string | null` | no | None |
| `slice_duration_sec` | `number | null` | no | None |


### `song_click_track`

Generate a click track WAV from an audio file.

        Uses beat_this (Foscarin et al., JKU) for beat + downbeat
        detection, then synthesizes sine-burst clicks at each beat with
        a higher-pitched accent on downbeats. Output is a mono WAV
        matching the source audio's length and samplerate so it can
        play in perfect sync in any DAW / audio player.

        Args:
            audio_path: source audio (wav/mp3/etc).
            output_path: destination WAV path. Defaults to
                ``<audio_dir>/click.wav``.
            checkpoint: beat_this checkpoint name (default ``"final0"``).
            device: torch device (``"cuda"``, ``"cpu"``, or None to
                auto-detect).
            use_dbn: enable the DBN post-processor for messier audio at
                ~2x inference cost.
            beat_freq_hz / downbeat_freq_hz: click pitch overrides.

        Returns ``{status, click_wav_path, duration_sec, samplerate,
        beat_count, downbeat_count, bpm_estimate}``.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `audio_path` | `string` | yes |  |
| `output_path` | `string | null` | no | None |
| `checkpoint` | `string` | no | 'final0' |
| `device` | `string | null` | no | None |
| `use_dbn` | `boolean` | no | False |
| `beat_freq_hz` | `number` | no | 1000.0 |
| `downbeat_freq_hz` | `number` | no | 1500.0 |


### `song_detect_beats`

Detect beat + downbeat timestamps in an audio file.

        Returns timestamps in seconds + a median-derived BPM estimate.
        No WAV is written — useful when you want to visualise or
        transform the beat grid without generating clicks.

        Args:
            audio_path: source audio.
            checkpoint: beat_this checkpoint (default ``"final0"``).
            device: torch device (``"cuda"``, ``"cpu"``, or None).
            use_dbn: DBN post-processor toggle.

        Returns ``{status, beats_sec, downbeats_sec, bpm_estimate,
        beat_count, downbeat_count}``.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `audio_path` | `string` | yes |  |
| `checkpoint` | `string` | no | 'final0' |
| `device` | `string | null` | no | None |
| `use_dbn` | `boolean` | no | False |


### `song_import_variations_to_live`

Drop variation wavs into the active Live set as new audio tracks.

        ``variations`` is the list returned by ``song_make_variations``
        under the ``variations`` key (or any list of ``{label, wav_path}``).
        For each: creates a fresh audio track named
        ``f"{track_name_prefix}{label}"`` and loads the wav into session
        clip slot ``clip_index`` via the bridge's ``browser.load_sample``.

        Per-variation failures are reported in the response rather than
        aborting the batch.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `variations` | `array<object>` | yes |  |
| `track_name_prefix` | `string` | no | 'Variation: ' |
| `clip_index` | `integer` | no | 0 |


### `song_load_wav_to_arrangement`

Drop a wav file onto a fresh arrangement audio track at ``position_beats``.

        Use this when you have a standalone wav on disk and want to run
        ``song_transpose`` / ``stems_split`` / ``song_make_variations``
        against it — those tools operate on the active Live arrangement,
        so the wav has to land in arrangement view first.

        Live 11's LOM doesn't expose a clean single-call "load wav into
        arrangement" — the bridge probes a few signature variants and
        returns ``status="not_supported"`` with a clear workaround if
        none work on your build. The temp track is auto-cleaned in that
        case so your session isn't littered.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `wav_path` | `string` | yes |  |
| `track_name` | `string | null` | no | None |
| `position_beats` | `number` | no | 0.0 |


### `song_make_variations`

Produce instrument-up remixes (or practice-pack tracks) from a stem set.

        Pass the ``stems`` list returned by ``stems_split`` directly.

        ``output_set="remix"`` (default) produces the conversational
        song-flow shape:

        - ``original.wav``     — sum at unity gain (sanity check)
        - ``instrumental.wav`` — sum without the vocals stem
        - ``<stem>_up.wav``    — full mix with one stem at +``boost_db`` and
                                 the rest at ``attenuation_db``. One file
                                 per stem (vocals included).

        With a 6-stem htdemucs_6s split that's 8 wavs.

        ``output_set="practice_pack"`` produces practice-track shape with
        two variants per non-vocal instrument (so a player can rehearse
        with or without the singer's part audible):

        - ``no_vocals.wav``                    — instrumental backing
        - ``<stem>_boost_no_vocals.wav``       — focal +boost, others -duck,
                                                  vocals dropped
        - ``<stem>_boost_with_vocals.wav``     — same focal/non-vocal balance
                                                  but with vocals at unity

        With a 6-stem split (5 instrument stems + 1 vocals) that's 11 wavs.

        ``name_prefix`` is prepended to every output filename — useful for
        batching multiple songs into one directory (e.g. ``"Reasons - "``).

        Pure file-on-disk math — no Live, no realtime. mp3 encoding is
        opt-in and best-effort.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `stems` | `array<object>` | yes |  |
| `output_dir` | `string` | yes |  |
| `boost_db` | `number` | no | 6.0 |
| `attenuation_db` | `number` | no | -3.0 |
| `output_set` | `string` | no | 'remix' |
| `name_prefix` | `string` | no | — |
| `encode_mp3` | `boolean` | no | True |
| `bitrate_kbps` | `integer` | no | 192 |


### `song_transpose`

Transpose the arrangement to ``target_key`` (high quality, not chipmunk).

        For every audio arrangement clip: enables warping, sets warp mode to
        Complex Pro (Live's gold-standard pitch-preserving engine), and adds
        the semitone delta to ``pitch_coarse`` (so any pre-existing pitch
        offset rides along). For every MIDI arrangement clip: shifts every
        note pitch by the delta. Then bounces the song. Then restores every
        snapshot so the original session state is preserved.

        Args:
            target_key: e.g. ``"F#"``, ``"Bb"``, ``"c minor"`` (case- and
                        suffix-tolerant; we only honour the tonic).
            source_key: explicit source. Omit to auto-detect via librosa
                        chroma. Detection is surfaced in the result so the
                        caller can re-call with an override if it was wrong.
            direction: ``"auto"`` (shortest signed path; tritone goes down),
                       ``"up"``, or ``"down"``.
            output_path: where to write the transposed wav. Defaults to
                         ``data/song_flow/<timestamp>/transposed_<key>.wav``.
            include_session: if True (default), also transpose every session
                             clip slot that has a clip. Required for sets
                             driven from Session view. Set False for
                             arrangement-only sets to skip the bridge calls
                             that enumerate session slots (small savings).
            min_duration_sec: floor for the bounce duration in seconds. Live's
                             ``/live/song/get/song_length`` has been observed
                             to under-report right after a set is opened
                             programmatically (e.g. via a synthesized .als),
                             silently truncating the bounce. Pass the source
                             clip's duration here to guarantee the bounce
                             captures the whole song.

        Realtime cost: ~30 s slice bounce (only when source_key is auto)
        plus the full song bounce.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `target_key` | `string` | yes |  |
| `source_key` | `string | null` | no | None |
| `direction` | `string` | no | 'auto' |
| `output_path` | `string | null` | no | None |
| `include_session` | `boolean` | no | True |
| `min_duration_sec` | `number | null` | no | None |



## Stems (stems_*)

### `stems_import_to_live`

[Phase 6] Create a fresh audio track per stem and report the mapping.

        `stems` is a list of dicts with keys `name` (vocals/drums/bass/other)
        and `path` (wav). For each, a new audio track named `Stem: <name>` is
        created in Live at the project tempo. Returns a list of
        {name, path, track_index} entries.

        Loading the wav INTO a clip on the new track requires the browser
        bridge (`browser_load_sample`) which is owned by another agent — the
        returned dict includes a clear `next_step` hint until that ships.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `stems` | `array<object>` | yes |  |
| `at_track_index` | `integer` | no | -1 |


### `stems_split`

[Phase 6] Run Demucs to split an audio file into stems.

        Defaults to ``n_stems=6`` (``htdemucs_6s``) which produces
        drums/bass/other/vocals + guitar + piano. The 6-stem split
        gives real per-instrument output and is preferred for downstream
        practice-track / boosted-stem workflows. Pass ``n_stems=4`` for
        the older ``htdemucs`` (drums/bass/other/vocals) if you want a
        smaller / faster split. Pass ``model="htdemucs_ft"`` (or any
        other Demucs model name) to override the n_stems default.

        Demucs runs on GPU when available (``torch.cuda.is_available()``)
        and falls back to CPU automatically. To enable GPU, install a
        CUDA torch build — see the docstring on
        ``ableton_mcp.stems.demucs.split_stems`` for the install commands.

        Output is written under ``data/stems/<model>/<basename>/<stem>.wav``.
        If demucs isn't installed in this venv, returns a structured error
        with the pip install hint.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `audio_path` | `string` | yes |  |
| `n_stems` | `integer` | no | 6 |
| `model` | `string | null` | no | None |



## Structure (structure_*)

### `structure_duplicate`

Insert a copy of the named section right after itself.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `structure_dict` | `object` | yes |  |
| `section_name` | `string` | yes |  |


### `structure_extend`

Add ``by_bars`` to the named section. Returns the updated structure.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `structure_dict` | `object` | yes |  |
| `section_name` | `string` | yes |  |
| `by_bars` | `integer` | yes |  |


### `structure_insert`

Insert a new section after the named anchor.

        Pass ``after_section_name=None`` (or empty string) to prepend at
        the start. ``role`` is auto-detected from ``new_section_name``
        when not supplied.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `structure_dict` | `object` | yes |  |
| `after_section_name` | `string | null` | yes |  |
| `new_section_name` | `string` | yes |  |
| `bars` | `integer` | yes |  |
| `role` | `string | null` | no | None |
| `notes` | `string` | no | — |


### `structure_jump_to_section`

Jump arrangement playback to the start of the named section.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `structure_dict` | `object` | yes |  |
| `section_name` | `string` | yes |  |


### `structure_loop_section`

Set Live's arrangement loop to span the named section.

        Sends ``/live/song/set/loop_start`` + ``loop_length`` (and
        optionally toggles loop on). Returns the beat range that was set.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `structure_dict` | `object` | yes |  |
| `section_name` | `string` | yes |  |
| `enable` | `boolean` | no | True |


### `structure_parse`

Parse the bar-counted dialect into a Structure dict.

        Accepts forms like ``"intro 4 / verse 8 / chorus 8"`` or
        ``"4 bars intro, 8 bars verse"``. An optional ``= N bars`` total
        validates the section sum. Returns ``{status, structure}`` where
        ``structure`` is the JSON form of :class:`Structure` (round-trip
        safe with the other ``structure_*`` tools).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `text` | `string` | yes |  |
| `time_signature_num` | `integer` | no | 4 |
| `time_signature_den` | `integer` | no | 4 |
| `tempo` | `number` | no | 120.0 |


### `structure_remove`

Remove the named section.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `structure_dict` | `object` | yes |  |
| `section_name` | `string` | yes |  |


### `structure_section_range`

Return ``{start_beat, length_beats, end_beat, start_bar, end_bar}``
        for the named section (does not touch Live).

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `structure_dict` | `object` | yes |  |
| `section_name` | `string` | yes |  |


### `structure_shrink`

Subtract ``by_bars`` from the named section.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `structure_dict` | `object` | yes |  |
| `section_name` | `string` | yes |  |
| `by_bars` | `integer` | yes |  |


### `structure_summary`

Multi-line musician-friendly summary of a structure.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `structure_dict` | `object` | yes |  |


### `structure_to_text`

Render a structure back to the canonical slash notation.

**Parameters**

| Name | Type | Required | Default |
|------|------|----------|---------|
| `structure_dict` | `object` | yes |  |
